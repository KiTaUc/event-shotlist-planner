# Event Shotlist Planner

Группирует обязательные кадры события по этапам и отмечает незакрытые позиции.

## Запуск

```bash
python src/event_shotlist_planner.py pending examples/sample.json
```

Локальный инструмент без внешних сервисов. Проверка: `python -m unittest discover -s tests -v`.
